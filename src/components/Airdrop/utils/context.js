
import { Context, createContext } from "react";

export const supplyContext = createContext(new Map());
export const balancesContext = createContext(new Map());
export const namesContext = createContext(new Map());
export const trackersContext = createContext(new Map());
export const poolsContext = createContext(new Map());
export const uwuPriceContext = createContext(0);
